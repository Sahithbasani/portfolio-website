import type { Metadata } from "next"; import { Callout } from "@/components/cards"; import { Breadcrumbs, FlowDiagram, PageHero } from "@/components/site-shell";
export const metadata:Metadata={title:"Production REST API Principles",description:"An inspected ASP.NET Core Orders API case study."};
export default function Page(){return <><Breadcrumbs items={[["Projects","/projects"],["Production REST API Principles"]]}/><PageHero eyebrow="Inspected code case study" title="Production REST API Principles" intro="A compact ASP.NET Core Orders API used to demonstrate contracts, HTTP semantics, idempotency, authorization, OpenAPI, and integration testing." meta="Public repository sample · Teaching implementation, not a production benchmark"/><article className="prose"><Callout title="What the code actually contains">A controller-based .NET API, in-memory order storage, validation attributes, JWT bearer setup, a policy-protected delete action, idempotency middleware, Swagger/OpenAPI assets, a Postman collection, and xUnit integration tests.</Callout><h2>Request path</h2><FlowDiagram steps={["HTTP request", "JWT auth", "Authorization", "Idempotency", "OrdersController"]}/><h2>Controller and HTTP semantics</h2><p><code>OrdersController</code> supports paged collection reads, lookup by integer ID, creation with <code>201 Created</code>, update with <code>204 No Content</code>, deletion behind an authorization policy, and a cancellation sub-resource that returns <code>202 Accepted</code> or <code>409 Conflict</code>.</p><pre><code>{`POST /api/v1/orders\nIdempotency-Key: checkout-42\n\n→ 201 Created\n→ a repeated completed key is rejected with 409`}</code></pre><h2>Idempotency: useful pattern, bounded claim</h2><p>The middleware uses a process-local concurrent dictionary. It rejects duplicate or in-flight keys and removes keys after failed requests. That demonstrates collision behavior, but production use would also need persistence, expiry, request fingerprinting, response replay policy, tenant scoping, and multi-instance coordination.</p><h2>Tests and contract assets</h2><p>The repository includes xUnit tests using <code>WebApplicationFactory</code>, an OpenAPI document, and a Postman collection. The integration test demonstrates conflict behavior for a repeated key. A production-ready suite would extend this to runtime/spec drift, authorization, validation, failure cleanup, and concurrent requests.</p><Callout title="Important code-review note" tone="amber">The repository README describes several production concerns as examples or future extensions. This page does not represent those roadmap items as already implemented.</Callout><h2>Production-readiness checklist</h2><ul><li>Move secrets out of in-memory configuration and enforce secure JWT validation.</li><li>Replace in-memory persistence with transactional data storage.</li><li>Use durable, scoped idempotency records with retention.</li><li>Add centralized Problem Details mapping, structured logs, metrics, traces, and health checks.</li><li>Generate OpenAPI from runtime behavior and fail CI when the checked-in contract drifts.</li></ul><p><a href="https://github.com/Sahithbasani/portfolio-website/tree/feature/sahith-dev-v1/production-rest-api-principles" target="_blank" rel="noreferrer">Inspect the source on GitHub →</a></p></article></>}
